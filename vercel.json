{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node",
      "config": { "runtime": "edge" }
    }
  ],
  "routes": [
    {
      "src": "/api/ref/(.*)",
      "dest": "/api/ref/[code].ts"
    }
  ]
}
